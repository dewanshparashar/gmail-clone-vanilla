- Create generator functions to generate dummy emails. @done
  - On running the function, download the dummy json @done
  - place the json in the data folder @done

2. Fix: CSS : Mails overflowing in because of some scroll heighting issue